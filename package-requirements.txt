# Dependencies, die das Sicherheitsmodul im Zielprojekt braucht
# (Express + Express-Types werden vorausgesetzt — die hat dein Projekt eh schon)

# Pflicht
zod                     >= 3.22.0     # validateBody
better-sqlite3          >= 11.0.0     # SqliteSecurityStore (nur falls SQLite genutzt)

# Dev / Types
@types/better-sqlite3   >= 7.6.0      # TypeScript-Typen für better-sqlite3
typescript              >= 5.0.0      # zum Bauen

# Im Zielprojekt vermutlich bereits installiert
express                 >= 4.18.0
@types/express          >= 4.17.0

# In Node.js Core (nichts zu installieren)
# - crypto.timingSafeEqual
# - crypto.createHash
# - crypto.randomUUID
