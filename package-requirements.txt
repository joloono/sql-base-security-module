# Dependencies required by the security module in the target project
# Express and Express types are expected to already exist in that project.

# Required
zod                     >= 3.22.0     # validateBody
better-sqlite3          >= 11.0.0     # SqliteSecurityStore, only when SQLite is used

# Development / types
@types/better-sqlite3   >= 7.6.0      # TypeScript types for better-sqlite3
typescript              >= 5.0.0      # build support

# Probably already installed in the target project
express                 >= 4.18.0
@types/express          >= 4.17.0

# Included in Node.js core; nothing to install
# - crypto.timingSafeEqual
# - crypto.createHash
# - crypto.randomUUID
